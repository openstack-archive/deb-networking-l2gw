#!/bin/sh
### BEGIN INIT INFO
# Provides:          neutron-l2gateway-agent
# Required-Start:    $network $local_fs $remote_fs $syslog
# Required-Stop:     $remote_fs
# Should-Start:      mysql postgresql rabbitmq-server keystone
# Should-Stop:       mysql postgresql rabbitmq-server keystone
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: Neutron L2 Gateway agent
# Description:       Provide L2 Gateway agent for Neutron
### END INIT INFO

# Author: James Page <james.page@ubuntu.com>

# PATH should only include /usr/* if it runs after the mountnfs.sh script
PATH=/sbin:/usr/sbin:/bin:/usr/bin
DESC="OpenStack Neutron L2 Gateway agent"
PROJECT_NAME=neutron
NAME=${PROJECT_NAME}-l2gateway-agent
# --config-file=/etc/neutron/neutron.conf will be happened
# to DAEMON_ARGS later by openstack-pkg-tools
DAEMON_ARGS="--config-file=/etc/neutron/l2gateway_agent.ini"
